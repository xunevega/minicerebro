services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: minicerebro
      POSTGRES_USER: minicerebro
      POSTGRES_PASSWORD: minicerebro
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:

